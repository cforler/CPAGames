import java.security.SecureRandom;

public class P3Adversary extends Adversary {
    private SecureRandom random = new SecureRandom();


 public boolean runAndGuess(BlockCipher bc) {
     return false;
 }
}

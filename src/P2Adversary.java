import java.security.SecureRandom;

public class P2Adversary extends Adversary {
    private SecureRandom random = new SecureRandom();


 public boolean runAndGuess(BlockCipher bc) {
     return false;
 }
}
